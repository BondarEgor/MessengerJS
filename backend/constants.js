const PATHS = {
  users: '/users.json',
  sessions: '/sessions.json',
  chats: '/chats.json',
  messages: '/messages.json',
};
const ONE_DAY = 1000 * 60 * 60 * 24;
const ONE_HOUR = 1000 * 60 * 60;

export { PATHS, ONE_DAY, ONE_HOUR };
